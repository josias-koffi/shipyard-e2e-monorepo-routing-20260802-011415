name: Web CI
on:
  pull_request:
    branches: [develop]
    paths: ['apps/web/**']
jobs:
  web:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: test -f apps/web/README.md
